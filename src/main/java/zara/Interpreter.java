package zara;

import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.List;

public class Interpreter {
    public void run(String sourceCode) {
        // TODO:
        // 1. Tokenizer tokenizer = new Tokenizer(sourceCode);
        // 2. List<Token> tokens = tokenizer.tokenize();
        // 3. Parser parser = new Parser(tokens);
        // 4. List<Instruction> instructions = parser.parse();
        // 5. Environment env = new Environment();
        // 6. for each instruction: instruction.execute(env);
    }

    public static void main(String[] args) {
        // TODO:
        // 1. Check args.length > 0, else print usage
        // 2. Read file: String code = new String(Files.readAllBytes(Paths.get(args[0])));
        // 3. new Interpreter().run(code);
    }
}
